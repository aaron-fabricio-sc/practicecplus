#include <iostream>
#include <string>

using namespace std;

int main()
{
    /*  int edad = 8;
     char letra = 'A';
     if (edad < 18)
     {
         cout << "eres menor de edad" << endl;
     }
     else
     {
         cout << "eres mayor de edad" << endl;
     }

     cout << letra << endl;

     int edades[] = {51, 8, 66};
     const char otra_letra = 'JS';
     cout << otra_letra << endl;
     edades[1] = 66;
     cout << edades[1] << endl;

     int a = 5;
     a += 1;
     int b = 8;
     bool c = a > b;
     cout << c << endl;
     cout << a << endl;

     cout << sizeof(edades) / sizeof(edades[0]) << endl;

     int ingresar = 0;

     cout << "ingresa tu edad" << endl;
     cin >> ingresar;
     cout << " tu edad es: " << ingresar << endl;

     int datos[] = {100, 200, 300};

     int limite = sizeof(datos) / sizeof(datos[0]);

     for (int i = 0; i < limite; i++)
     {
         cout << datos[i] << endl;
     }

     int par = 0;
     cout << "Ingresa hasta que numero quieres verificar";
     cin >> par;

     for (int i = 0; i < par; i++)
     {
         if (i % 2 == 0)
         {
             cout << i << endl;
         }
     }

     while (true)
     {
         char respuesta;
         cout << "Quieres salir del programa??" << endl;
         cin >> respuesta;

         if (respuesta == 'y')
         {
             cout << "bye bye!" << endl;
             break;
         }
     }

     string saludo = "hola a toodos";

     cout << saludo.size(); */
}